#include<stdio.h>

struct node{
	int data;
	struct node *next;
};

struct node addlast_edge(){
	struct node
}

int printheads(A[], int ){
	
}

int main(){
	struct node *head[5]={NULL};
	heads[0]=addlast_edge(head[0],1);
	return 0;
}
